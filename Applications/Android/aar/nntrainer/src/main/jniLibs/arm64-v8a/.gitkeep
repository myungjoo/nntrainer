# Placeholder so git tracks the empty jniLibs/arm64-v8a directory.
# tools/package_android.sh + the gradle copyNntrainerJniLibs task
# populate this directory with the nntrainer .so set at build time.
